version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: postgres_i12
    environment:
      POSTGRES_USER: i12user
      POSTGRES_PASSWORD: i12pass
      POSTGRES_DB: i12db
    volumes:
      - ./postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - i12-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend_i12
    working_dir: /app
    volumes:
      - ./backend:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_RUN_HOST=0.0.0.0
    ports:
      - "5000:5000"
    depends_on:
      - postgres
    networks:
      - i12-network

  nginx:
    image: nginx:latest
    container_name: nginx_i12
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend
    networks:
      - i12-network

networks:
  i12-network:
